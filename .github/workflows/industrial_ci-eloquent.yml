name: industrial_ci (eloquent)

on:
  push:
    branches:
      - eloquent-devel
    paths-ignore:
      - 'docs/**'
      - '**.md'
  pull_request:
    branches:
      - eloquent-devel
    paths-ignore:
      - 'docs/**'
      - '**.md'
  schedule:
    - cron: "0 2 * * 0" # Weekly on Sundays at 02:00

jobs:
  industrial_ci:
    strategy:
      matrix:
        env:
          - { ROS_DISTRO: eloquent, ROS_REPO: ros }
          - { ROS_DISTRO: eloquent, ROS_REPO: testing }
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: "ros-industrial/industrial_ci@master"
        env: ${{ matrix.env }}